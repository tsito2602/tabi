# tabi Design System

> Neutral surfaces. Blue actions. A trip you can arrange by hand.

旅行情報をふたりで集め、予定を並べ、旅先で読めるアプリ。オフライン保存・共同編集を前提とし、装飾より予定の読みやすさと操作の連続性を優先する。

## 1. 一つの視覚・操作言語

- 背景・文字・罫線・通常カード・未選択コントロールは白黒とグレー。青い台紙や濃紺のダーク背景を使わない。
- tabiの青は主要操作、選択、リンク、フォーカス、ドラッグの挿入先に使う。通常のタイトル・時刻・分類アイコンは無彩色。
- 同じ情報は、候補・予定・詳細の間で関係が分かる形で表示する。すべてを同じ大きな箱にすることは統一ではない。
- 通常時は静かにし、操作した対象だけに動きと浮き上がりを与える。常時動く装飾、強いバウンド、本文のブラーは使わない。
- 写真とチケット表現は旅行の入口に集中。予約一覧は読みやすい情報カード、原本は詳細から開く。架空のQR・便名・券面番号を作らない。

参考：coss uiは共通部品、Beautiful UIは情報のまとまり、Kineticsは操作の手応えの参考とする。3ライブラリの一括導入・無条件な依存追加はしない。

## 2. 色

色の正は `src/constants/design.ts`。Web初期表示は `src/redesign.css`、PWAメタ色は `src/app/+html.tsx` と manifest で合わせる。konogoroの白黒を土台とし、オレンジは持ち込まない。

| 役割 | Light | Dark |
| --- | --- | --- |
| canvas | #F7F7F5 | #141416 |
| paper | #FFFFFF | #202023 |
| ink | #242424 | #F5F5F3 |
| mist | #EFEFEC | #2B2B2F |
| soft | #F3F3F0 | #252528 |
| ash（境界） | #DDDDD7 | #3B3B40 |
| smoke（補助本文） | #62625F | #B8B8B2 |
| slate（二次本文） | #555552 | #C6C6C0 |
| ocean（主要操作・選択） | #496B80 | #A1BDCC |
| onOcean | #FFFFFF | #141416 |
| sky（選択・配置先の面） | #E8EEF2 | #28343B |

エラー・警告・状態アイコンの意味色は例外。色だけでは状態を伝えない。通常の大きなパネルを状態色で塗らない。境界線は装飾、操作対象の識別・選択には文字・形・明瞭なフォーカスを併用する。

## 3. 部品・文字・余白

| 部品 | 基準 |
| --- | --- |
| 標準カード | 16px角丸、1px境界、paper、内側16px。通常は影なし |
| ボタン・入力 | 12px角丸、タップ44px以上。入力文字16pxを基準 |
| 短い確認ダイアログ | 20px角丸、必要な高さだけ |
| 旅行入口 | 写真と24px程度の丸み。①の開閉と関係を保持 |
| 見出し | 22〜24px / 600、行高1.35程度 |
| カードタイトル | 17px / 600、25px行高。長文・文字拡大で伸ばす |
| 本文 | 15〜16px。補足は12〜13px以上を基準 |
| 時刻 | 桁揃えのmono、13px以上。青で一律強調しない |
| 余白 | 4/8/12/16/24px。スマホの左右20px |

共通実装：`ui/surface-card.tsx` と `ui/action-button.tsx`。カードの内容は `CardContent`、外形は `SurfaceCard` が所有する。影は持ち上げたカード、メニュー、PCの重なった面など浮いている意味がある場面だけ。写真以外の装飾グラデーションは使わない。

## 4. ナビゲーション

旅行一覧を最上位とし、旅行内は「しおり／場所／予約／準備／メモ」。場所の読み上げ名は「行きたい場所」。スマホの文字中心タブとPCサイドバーは `trip-navigation.ts` を共有する。メンバー管理は旅行の操作、アプリ設定は旅行の外に置く。

旅行内タブは細い選択表示、日付は小さな選択コントロールとし、大きなピルを二重に重ねない。ページ名の大見出しを繰り返さず本文の面積を確保する。日付ごとに別ページへ分割せず、同じしおり内へスクロールする。

## 5. しおりと編集状態

- 予定は「左の時刻欄＋右の角丸カード」。アイコンとタイトルはカード内。独立した大きいアイコン列は廃止する。
- 時刻の表示が外にあっても、時刻は予定に属する。縦距離を時間に比例させるカレンダーではない。
- 通常の予定同士に線を引かない。乗り継ぎ、予約の出発／到着、予定間の移動など意味のある関係だけを表す。
- 移動はコンパクトな接続行。通常の候補と同じ大きなカードへ膨らませない。
- 「予定を組む」は同じしおりの編集状態。モード切替で読んでいた日・スクロール・入力データを作り直さない。
- PCは右に候補一覧、スマホは下にしおりと並ぶ候補欄。背景を暗くするボトムシートではない。候補は検索・展開できる。

### 配置

Web/PWAはドラッグ用ハンドルからマウス・ペンで持ち上げる。タッチはハンドルを長押しして開始し、カード本文では通常のスクロールと詳細表示を妨げない。候補・予定を選んで配置先をタップする操作、キーボードのボタン操作でも同じコマンドを使える。ネイティブ版は選択→配置の代替操作を持つ。

ドラッグ中のカードはポインターへ直接追従。挿入位置に空きを作り、離してから保存・短い到着表示を行う。画面端のスクロール、日付に重ねての日移動、Escape・画面離脱・領域外ドロップによるキャンセルを扱う。キャンセルでデータを書かない。

候補の場所は残し、配置済みなら既存予定への入口を表示する。時刻未定の予定は訪問順だけを記録し、勝手に時間を割り当てない。既存の時刻と矛盾する位置だけ調整を確認する。予定の終了時刻は長さを保持して動かし、未定にする場合は開始／終了を明示的に解除する。予約の日時はドラッグで変更しない。

移動と追加の取り消しは直前操作を対象にする。キャッシュ全体を過去へ巻き戻さず、現在取得できている内容が変わっていたら停止する。サーバーでの競合制御や別端末同時編集の原子的な保証と混同しない。

## 6. 保存・互換性

`ItineraryDetails.placement` に配置先の日／時刻、前後の行キー、論理順序、項目間の安定した配置キーを追加する。連続した移動で他のカードの順序を崩さず、配置キーは実時刻を書き換えない。既存JSON格納と同期経路を使い、古いデータに移行を強制しない。予約のレコード・時刻は書き換えない。

旧クライアントの更新でplacementを省略してもサーバー側の配置情報を消さない。明示的nullは解除。変更された日時、削除された起点、不正なメタデータは時系列の既存規則へ安全に戻す。並び順は同じデータから再現できるものとし、DOM順や端末だけの状態を正にしない。

③の「しおりへ」は日時指定とその日のプレビューの入口として残す。ドラッグを強制しない。オフラインのキュー、閲覧権限、場所の元情報と原本へのアクセスを維持する。

## 7. 詳細・編集・準備・メモ

スマホの詳細・追加・編集・日付選択・メモは②の全画面。三点メニューと短い削除／破棄確認はコンパクトな例外。元のカードから不透明な面が展開し、意味のあるまとまりが順に現れ、閉じると戻る。タイトルや時刻だけの分離移動はしない。登場完了を入力・閉じる操作の前提にしない。

準備は密度のあるチェック行。メモ本文は書くための一枚の面。両者を大きいタイルへ一律変換しない。設定・入力・メニューも同じ白黒と操作色・境界・文字の規則を使う。コピーはボタン内、保存失敗は消えないエラー、通常同期は控えめな表示を継続する。

## 8. アクセシビリティ・検証

フォーカスを見失わない。色だけで選択を表さず、読み上げラベル・状態も更新する。reduced-motionでは挿入位置の寸法変化・装飾移動を省き、操作の結果を文字や静かな強調でも知らせる。ドラッグの代替に実際のタップ可能な44px領域を用意する。

静的な色値・単体テストと実ブラウザ／実機検証を区別する。データ／並び順／保存と取り消しの回帰を検証し、最終差分の `npm run check` を実行する。環境上実行できないものを成功と報告しない。実機の見た目・キーボード・ドラッグの確認はユーザー側で行う方針を維持する。
